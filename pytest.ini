[pytest]
DJANGO_SETTINGS_MODULE = config.settings.local
python_files = tests.py test_*.py *_tests.py
markers =
    postgres: requires PostgreSQL transactional locking
    clamav_integration: requires a reachable real ClamAV service
